@model ShoppingApp.Models.Category

@{
    ViewBag.Title = "Browse";
}

<h2>Selected Categoey: @Model.cat_name</h2>

<ul>
    @foreach (var item in Model.Items) { 
    <li>
        @Html.ActionLink(item.Title, "Details", new { id = item.ItemId})
    </li>
    }

</ul>

